{
  "name": "anvil",
  "version": "0.1.0",
  "private": true,
  "description": "Idempotent webhook to BullMQ worker pipeline.",
  "license": "MIT",
  "author": "Lakshyaraj Singh Rao",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
