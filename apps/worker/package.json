{
  "name": "@anvil/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@ykstormsorg/anvil": "workspace:*",
    "bullmq": "^5.34.5",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "ioredis-mock": "^8.9.0"
  }
}
